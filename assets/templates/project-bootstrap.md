# Project Bootstrap Record

- Project ID: `<project-id>`
- Project name: `<project-name>`
- Repository identity: `<repository-identity>`
- Project Brain root: `.aidev`
- Global Skill: `yyz-dev-os`
- Loaded compatible Skill version: `2.8.1`
- Configured storage root resolver: `<resolver>`

## Checklist

- [ ] Observe repository identity and default branch.
- [ ] Replace every placeholder in `.aidev` templates.
- [ ] Validate `recovery-entry.json` against the bundled schema.
- [ ] Define and run project-owned schemas or deterministic validators for Operating Rules and authoritative Project Brain records.
- [ ] Define authority and recovery order.
- [ ] Set `doNotUseChatMemoryAsProjectTruth` to `true`.
- [ ] Define verification commands, protected paths, approvals, promotion, release, backup, and rollback.
- [ ] Create architecture, checkpoint, evidence, and Capability Ledger bindings when required.
- [ ] Verify that no secret or machine-specific absolute path was stored.
- [ ] Test recovery using durable state only.

Bootstrap status: `INCOMPLETE` until every required item is evidence-backed.
